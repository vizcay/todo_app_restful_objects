// Generated by CoffeeScript 1.8.0
(function() {
  var __bind = function(fn, me){ return function(){ return fn.apply(me, arguments); }; },
    __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  $((function(_this) {
    return function() {
      return _this.TaskRowView = (function(_super) {
        __extends(TaskRowView, _super);

        function TaskRowView() {
          _this.on_delete = __bind(_this.on_delete, this);
          _this.on_edit = __bind(_this.on_edit, this);
          _this.on_mark_done = __bind(_this.on_mark_done, this);
          _this.render = __bind(_this.render, this);
          return TaskRowView.__super__.constructor.apply(this, arguments);
        }

        TaskRowView.prototype.tagName = 'tr';

        TaskRowView.prototype.events = {
          'click #mark_done': 'on_mark_done',
          'click #edit': 'on_edit',
          'click #delete': 'on_delete'
        };

        TaskRowView.prototype.template = _.template($('#task_row_template').html());

        TaskRowView.prototype.render = function() {
          this.$el.html(this.template(this.model.attributes));
          return this;
        };

        TaskRowView.prototype.on_mark_done = function() {
          this.model.set({
            completed: true
          });
          return this.model.save({
            success: Backbone.history.loadUrl()
          });
        };

        TaskRowView.prototype.on_edit = function() {
          return app.navigate("task_view/" + (this.model.get('id')), {
            trigger: true
          });
        };

        TaskRowView.prototype.on_delete = function() {
          if (confirm("Are you sure to delete task #" + (this.model.get('task_id')) + "?")) {
            return this.model.destroy({
              wait: true,
              success: function() {
                return Backbone.history.loadUrl();
              }
            });
          }
        };

        return TaskRowView;

      })(Backbone.View);
    };
  })(this));

}).call(this);
